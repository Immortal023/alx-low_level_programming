#include <stdio.h>

/**
 * main - prints alphabetics character
 * Return: Always 0 (success)
 *
 */

int main(void)
{
char l;

for (l = 'a' && l != 'q' ; l <= 'z' && l != 'e' ; l++)
putchar(l);
return (0);


}
