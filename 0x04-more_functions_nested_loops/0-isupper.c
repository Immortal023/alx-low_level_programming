#include "main.h"
#include <stdio.h>

/**
 * int _isupper - uppercase character.
 * @c: char to check
 * Return: 0 or 1.
 */
int _isupper(int x)
{
if (x >= 65 && x <= 90)
{
return (1);
}
return (0);
}
