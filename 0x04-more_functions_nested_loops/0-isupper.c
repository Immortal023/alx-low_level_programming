#include "main.h"
#include <stdio.h>

/**
 * int _isupper - uppercase character.
 * @c: char to check
 * Return: 0 or 1.
 */
int _isupper(int c)
{
	if (c >= 65 && C <= 90)
		return (1);
	else
		return (0);
}
